target-version = "py310"
line-length = 100

[lint]
select = ["E", "F", "I", "B", "UP"]
ignore = [
    "E501",   # line length — handled by formatter
    "UP007",  # X | Y union syntax — fine in 3.10+ but mixed with __future__ already
]
